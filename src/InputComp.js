import React from "react";

export default function InputComp(props){
    return(
        // <input type={props.type} name={props.name} placeholder = {props.placeholder} required style={{background:'#e9ecf2',padding:5,borderRadius:5,width:'250px',borderColor:'#e9ecf2',textAlign:'center'}}/>
    )
}